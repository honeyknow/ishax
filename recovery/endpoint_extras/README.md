# Endpoint Extras

Nonessential endpoint testing assets moved out of the active endpoint package.

| Item | Purpose |
|---|---|
| `ART_Offline/` | Offline Atomic Red Team test library. Bulky and not required for deployment. |
| `Automated_Test_Runner.ps1` | Test runner that expects the offline ART layout. |

Use this folder only when intentionally running lab validation tests.

