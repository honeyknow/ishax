# Legacy

Historical rules and manifests retained for reference only.

These files are not the active detection source unless they are explicitly moved back into `server/pipeline/sigma_rules` and reviewed against the locked scope.

| File/folder | Purpose |
|---|---|
| `legacy-manifest.md` | Historical manifest from an earlier rebuild. |
| `t1047-wmi.yml` | Old WMI rule. Out of current scope. |
| `t1105-ingress-tool-transfer.yml` | Old ingress/tool-transfer rule. Out of current scope. |
| `sigma_rules/` | Historical rule staging folder. |

