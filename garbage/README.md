# Garbage / Scratch

This folder contains old debug scripts, raw dumps, logs, and scratch files.

Nothing in this folder should be required to start the lab, deploy an endpoint, run the backend, or build the frontend. Treat files here as recovery/debug material only.

Before deleting anything permanently, move important files to `recovery/` or document why they are no longer needed.

